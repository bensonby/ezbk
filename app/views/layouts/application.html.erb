<!DOCTYPE html>
<html>
  <head>
    <title>Personal Finance Management</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag 'autocomplete-rails.js'  %>
    <%= javascript_include_tag 'jquery-ui-1.8.20.custom.min' %>
    <%= javascript_include_tag 'jquery.hotkeys' %>
    <%= stylesheet_link_tag 'jquery-ui-1.8.20.custom.css' %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body class="<%= @page_name %>">
    <div id="container">
      <%= render :partial => 'layouts/menu' %>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}", :class => "flash" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
    <%= yield(:javascripts) %>
    <script type="text/javascript">
      <%= hotkey_for_path_js('t', transactions_path) %>
      <%= hotkey_for_path_js('shift+t', new_transaction_path) %>
      <%= hotkey_for_path_js('a', accounts_path) %>
      <%= hotkey_for_path_js('shift+a', new_account_path) %>
      $(document).bind('keyup', 'q', function(evt){ 
        $('#transaction_date').focus();
      });
      <% # TODO: set hotkey for help '?' %>
    </script>
  </body>
</html>
