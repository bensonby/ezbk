<%= form_for(@transaction_entry) do |f| %>
  <% if @transaction_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction_entry.errors.count, "error") %> prohibited this transaction_entry from being saved:</h2>

      <ul>
      <% @transaction_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %>
    <%= f.number_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :debit_amount %>
    <%= f.text_field :debit_amount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
